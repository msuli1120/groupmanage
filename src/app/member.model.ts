export class Member {
  constructor(public name: string, public age: number, public sex: string, public bio: string, public role: string, public joinDate: string, public location: string, public professionAge: string, public picture: string) {}
}
